1.
./mv01 file1.txt file2.txt 명령어를 넣었을 때, file2.txt는 일반 문자열이기 때문에
이게 디렉토리인지 아닌지 어떻게 판단할 수 있을까 고민했음
그러나 stat함수에 문자열을 그대로 넣으면 이게 해당 폴더 안에 있는지 없는지 판단해서 0 혹은 -1을 리턴해줌.
만약 존재한다면 디렉토리인지 아닌지 판단해주면 되고, 존재 하지 않는다면, rename 해주면 된다.